fastapi
uvicorn[standard]
pydantic[email]
motor
passlib[bcrypt]  # Ensures bcrypt extras are installed with passlib
python-multipart # For FastAPI form data (OAuth2PasswordRequestForm)
python-jose[cryptography] # For JWT tokens
httpx
# For Ansible integration (to be added later if direct execution is needed)
# ansible-runner

